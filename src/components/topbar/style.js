import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(theme => ({
  mainBar: {
    background: '#8282ff',
    border: 0
  },
  btn_auth: {
    textTransform: 'none'
  },
  topbar: {
    padding: '0 20px',
    background: '#fff'
  }
}));
