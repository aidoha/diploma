import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  company_toolbar: {
    background: '#fff',
    color: '#33333e',
    width: '100%',
  },
  service_arrow_back: {
    cursor: 'pointer',
    marginRight: '20px',
  },
  logout: {
    cursor: 'pointer',
  },
});
